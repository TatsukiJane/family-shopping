# В репозитории всегда LF: разработка идёт на Windows, сборка — на Linux в CI.
# Без этого каждый чекаут порождал бы диффы из-за перевода строк.
* text=auto eol=lf

*.png binary
*.ico binary
*.woff2 binary
